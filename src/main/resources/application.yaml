server:
  port: 8400

logging:
  level:
    org.springframework: DEBUG

spring:
  devtools:
    add-properties: true
  application:
    name: mybank-loans-service
  datasource:
    url: jdbc:postgresql://localhost:5432/loans
    username: admin
    password: password
  jpa:
    show-sql: true
  redis:
    host: localhost
    port: 6379

